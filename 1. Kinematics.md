## What this section if for:

- Which **kinematic equation** to choose?
    
- When do I **not** use kinematics (because acceleration isn’t constant)?

Kinematics describes **motion without considering forces**. Valid when:
✔ Acceleration is **constant**  
✘ Forces changing in time or drag forces dominate (then use Newton’s Laws or energy methods)

## 📐 Vector Components 

If a velocity v has magnitude and angle THETA
$$
v_x=vcos⁡θ,v_y=vsin⁡θ
$$

Total magnitude:$$v = \sqrt{v_x^2 + v_y^2}$$
## ​Kinematic Equations ##
(constant acceleration)
For any dimension (x or y):
$$
v = v_0 + at
$$
$$
x = x_0 + v_0t + \frac{1}{2} at^2
$$
$$ v^2 = v^2_0 + 2a(x - x_0)$$
$$ x = x_0 + \frac{1}{2} (v_0 + v)t$$
These apply independently in x and y.
## Projectile Motion ##
Often:
$$ a_x = 0, a_y = -g $$
Horizontal: 
$$ x = v_0 t $$
Vertical:
$$ y = v_0t - \frac{1}{2} g t^2 $$
Time of flight (when y = 0)
$$ t = \frac{2v_0}{g} $$
Range (no air resistance):
$$ R = \frac{v_0^2 sin⁡(2θ)}{g} $$
## 📌 When NOT to use kinematics

Don’t use these equations when:  
❌ Drag/air resistance is significant  
❌ Mass changes (rocket)  
❌ Acceleration is not constant  
Use **forces (Newton’s laws)** or **energy methods** instead.
